Description List: {description_list}
Recent Events: {recent_events}
Current Perception: {current_perception}

Action Types:
Each description must be assigned exactly one of these four actions:
- chat: assign only when the description explicitly or implicitly mentions another person, AND Current Perception contains at least **two or more communicable persons**.
- interact: assign only when the description explicitly or implicitly involves a specific object (item), AND Current Perception contains at least one visible item.
- move: assign only when the description explicitly or implicitly describes physical movement toward or between specific locations.
- think: assign when none of the above conditions are met. Use for internal thoughts, plans, or general intentions.

Rules:
- If the Current Perception contains fewer than two communicable persons, you must not assign "chat", even if the description mentions a person.
- If the Current Perception contains no visible items, you must not assign "interact", even if the description mentions an object.
- Always check Current Perception first before assigning actions.
- Assign exactly one action type to each description, no multiple labels.


Output:
Return a JSON array of objects. Each object must include the assigned action and the original description.
Do not include any additional text, markdown formatting, or explanations outside the JSON array.

[
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}}
]