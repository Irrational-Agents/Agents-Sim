Description List: {description_list}
Recent Events: {recent_events}

Action Types:
Each description must be assigned exactly one of these 4 actions:
- chat: used only when another person is explicitly or implicitly mentioned in the description.
- interact: used only when a specific object is explicitly or implicitly involved in the action (e.g., interacting with an item like desk, toilet).
- move: used only when a physical movement toward or between specific locations is explicitly or implicitly described.
- think: used for internal thoughts, plans, or general intentions when no specific person or object is mentioned.

Please assign one of these four action types to each description in the Description List.
Format your response as a JSON list of objects, where each object contains the action type and the original description. Do not include any additional text, markdown formatting, or explanations outside the JSON array:
[
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}},
{{"action": "action_type", "description": "original description"}}
]